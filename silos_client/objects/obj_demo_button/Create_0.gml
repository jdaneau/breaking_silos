/// @description DEMO: Initialize button

// Disable animating the sprite (frames are used for mouse hover instead)
image_speed = 0;
image_index = 0;

// Initialize button label (set by `obj_demo_xtend`)
label = "";

// Initialize button function when clicked (set by `obj_demo_xtend`)
action = undefined;